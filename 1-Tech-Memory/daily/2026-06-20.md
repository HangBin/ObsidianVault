# 2026-06-20 星期六

## 11:36 - 安装 FreeLLMAPI 项目
**触发**: 老板要求安装 https://github.com/tashfeenahmed/freellmapi 到 /home/ 目录
**工具**: web_fetch, exec, read, write, edit
**结果**: 项目克隆到 /home/freellmapi，完成依赖安装和配置
**决策**: 
- 无 Docker，使用 npm 源码安装方式
- 系统 GCC 9.4 不支持 C++20，安装 GCC-10 解决 better-sqlite3 编译问题
- 生成 64 位 hex 加密密钥，配置 .env

## 11:37 - 编译 better-sqlite3 失败
**触发**: npm install 报错 `g++: error: unrecognized command line option '-std=c++20'`
**工具**: exec (apt-get install gcc-10 g++-10)
**结果**: 安装 GCC 10.5.0 后编译通过
**决策**: 系统自带 GCC 9.4 不支持 C++20，better-sqlite3 需要 C++20

## 12:08 - npm install 超时
**触发**: npm install 下载依赖太慢，进程多次被 SIGKILL
**工具**: exec (调整超时策略，避免管道导致的 exit code 混乱)
**结果**: 最终用 CC=gcc-10 CXX=g++-10 npm install 成功安装 735 个包
**决策**: 不用管道 `| tail -20`，避免 exit code 被掩盖

## 12:39 - 服务启动成功
**触发**: npm run dev 启动开发服务器
**工具**: exec
**结果**: 
- Dashboard: http://localhost:5173
- API: http://[::]:3001/v1/chat/completions
- 69 个模型已加载
- 统一 API Key: freellmapi-36de985aea262e45a78f9430d5d21129cc33a2e0a82d8806
**决策**: 服务启动正常，但需要先配置上游 Provider Key

## 14:32 - 配置 Dashboard 账号和上游 Key
**触发**: 老板提供邮箱 panbin521@sina.com、密码 5201314、OpenRouter Key
**工具**: exec (curl API)
**结果**: 
- Dashboard 账号创建成功（密码需要 8 位，用 5201314a）
- OpenRouter Key 添加成功但验证失败（401 User not found）
**决策**: 
- 原密码 5201314 只有 7 位，系统要求至少 8 位
- OpenRouter Key 无效，需要老板确认

## 15:02 - 修复 keyless Provider 无法使用
**触发**: Dashboard "试完台" 报错 "All models rate-limited. OpenRouter API error 401"
**工具**: exec (sqlite3, node.js 脚本)
**结果**: 
- 发现 keyless Provider (Pollinations/LLM7/Kilo) 在数据库里没有 api_keys sentinel 记录
- 通过 node.js 脚本直接往数据库插入加密的 keyless sentinel 记录
- 禁用无效的 OpenRouter Key
**决策**: 
- keyless Provider 需要在 api_keys 表中有 sentinel 记录（key='no-key'，AES-256-GCM 加密存储）
- OpenRouter Key 已禁用避免 401 干扰

## 15:22 - 配置局域网访问
**触发**: 老板要求 Dashboard 支持 localhost 和局域网 192.168.1.210 访问
**工具**: edit (修改 vite.config.ts 和 .env)
**结果**: 
- Vite 添加 host: '0.0.0.0', port: 5173
- .env 添加 HOST_BIND=0.0.0.0 和 DASHBOARD_ORIGINS=http://192.168.1.210:5173
- 验证两个地址都返回 200，CORS 头正确
**决策**: 需要监听所有接口才能局域网访问

## 15:22 - API 调用测试成功
**触发**: 测试 auto 路由是否正常工作
**工具**: exec (curl)
**结果**: 
- auto 路由 → Pollinations GPT-OSS 20B 正常响应
- 1+1=2 回答正确
- 路由信息: {platform: pollinations, model: openai-fast}
**决策**: keyless Provider 配置成功，系统可用

## 15:48 - 记忆写入和完整性检查
**触发**: 老板要求写入今日记忆并检查完整性
**工具**: write, exec
**结果**: 创建 memory/2026-06-20.md，写入全天记录
**决策**: 按 AGENTS.md 对话结束协议执行

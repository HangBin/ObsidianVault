---
date: 2026-06-12
weekday: Thursday
tags:
  - daily-log
  - media-agent
created: 2026-06-12T11:20:00+08:00
modified: 2026-06-12T11:20:00+08:00
---

# 2026-06-12 日志

## 11:20 - 闲鱼 Cookie 提取
**触发**: 用户要求通过 CDP 方式连接闲鱼，用户登录后提取 cookies
**工具**: CDP (Chrome DevTools Protocol) via port 9222/9223, websocket-client, curl
**结果**: ✅ 成功提取 18 个 cookies，识别出 7 个关键 cookies
**决策**:
- 浏览器 headless 模式无法让用户手动登录，需要用户自行启动有界面浏览器
- 用户在本地 Chrome 登录后通过 CDP 提取 cookies
- 关键 cookies: havana_lgc2_77, cna, sgcookie, _tb_token_, havana_lgc_exp, tracknick(panbin5218), unb(748552523)
- Cookies 保存位置:
  - `/root/.openclaw/workspace-media/.config/xianyu_cookies.txt` (完整 cookie string)
  - `/root/.openclaw/workspace-media/.config/xianyu_key_cookies.json` (关键 cookies)
  - `/tmp/goofish_cookies.json` (完整 JSON)
  - `/tmp/goofish_cookies.txt` (Netscape format)

## ⚠️ 浏览器管理经验教训
- snap 版 Chromium (9223端口) 和 google-chrome (9222端口) 可能同时存在
- headless 模式下用户无法看到页面，需要 `--remote-allow-origins=*` 才能接受 WebSocket 连接
- 端口占用时需要用 `fuser -k <port>/tcp` 清理
- chrome extension background.html 页面会占用 CDP 端口，干扰判断

## 11:44 - 闲鱼商品发布成功

**触发**: 用户要求用提取的 cookies 完成昨天未完成的闭环任务
**工具**: CDP (Chrome DevTools Protocol), DOM 操作, base64 图片传输
**结果**: ✅ 第一个商品"DeepSeek V4 API 包月畅用"发布成功！
**决策**:
- 通过 CDP 的 DOM.setFileInputFiles + base64 编码图片注入，成功绕过闲鱼上传组件的 React 事件限制
- 关键突破：将图片 base64 编码后分块传输到浏览器，在页面内创建 Blob/File 对象并触发 change 事件
- 计价方式选择"元/次"，服务类型选择"指令优化"
- 发布页面无标题输入框，标题自动从描述中提取
- 商品已上架：¥4，包邮，DeepSeek服务分类

### 发布结果
- 商品：DeepSeek V4 API 包月畅用
- 价格：¥4（到手 ¥3.98，扣除 0.6% 服务费）
- 分类：DeepSeek服务 / 元/次 / 指令优化
- 状态：已上架，1浏览

### 待完成
- 第二个商品"opus4.8全网底价0.09/刀"尚未发布（脚本中配置了两个商品）

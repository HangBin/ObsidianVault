---
date: 2026-06-11
weekday: Wednesday
tags:
  - daily-log
  - media-agent
created: 2026-06-11T13:29:00+08:00
modified: 2026-06-11T13:50:00+08:00
---

# 2026-06-11 日志

## 13:29 - publish.py 报错修复

**触发**: 老板反馈 publish.py 运行报错
**工具**: exec、read、edit、Playwright 调试

### 问题诊断

**报错1：`Missing X server`**
- 原因：`headless=False` 在无头服务器上无法启动
- 修复：改为 `headless=True`

**报错2：页面加载超时（60秒无 input/contenteditable）**
- 原因1：`wait_until='networkidle'` 在闲鱼 SPA 上永远等不到
- 原因2：Cookie 未生效，页面显示"立即登录"
- 修复：改为 `wait_until='domcontentloaded'` + 增加等待时间到 8 秒

**报错3：Cookie 中缺少 `a1` 字段**
- `a1` 是闲鱼的 HttpOnly 核心登录凭证
- Cookie 中只有 21 个字段，缺少关键的 `a1`
- CDP 确认浏览器 session 中也没有 `a1`

### 根因分析（6月10日已确认）

1. **Cookie 缺少 `a1`** — HttpOnly cookie，代码注入无法设置
2. **服务器 IP 被闲鱼风控** — 机房 IP 被拦截（240e:36f:...）
3. 6月10日的截图也证实当时未登录（所有截图都是扫码登录页面）

### 代码修复

1. ✅ `headless=False` → `headless=True`
2. ✅ `wait_until='networkidle'` → `'domcontentloaded'`
3. ✅ 增加登录状态检测逻辑（检测"扫码安全登录"/"短信登录"/"立即登录"）
4. ✅ 增加文件上传控件检测（`input[type="file"]`）
5. ✅ 延长 SPA 渲染等待时间（5s → 8s）

### 当前状态

| 问题 | 状态 | 说明 |
|------|------|------|
| headless 模式 | ✅ 已修复 | `headless=True` |
| 页面加载超时 | ✅ 已修复 | 改 `domcontentloaded` + 登录检测 |
| Cookie 缺少 `a1` | ❌ 无法代码修复 | 需从浏览器提取完整 Cookie |
| 服务器 IP 风控 | ❌ 无法代码修复 | 需住宅 IP 或代理 |

### 待老板决策

- 方案A：老板在本地电脑运行（住宅 IP + 有头模式扫码登录一次）
- 方案B：获取完整 Cookie（含 `a1`）+ 住宅代理
- 方案C：探索闲鱼开放平台 API 发布（不走浏览器）

**决策**: 等待老板确认方案

## 14:16 - publish.py 修复完成 & 测试通过

**触发**: 老板要求修改后测试通过再报告
**工具**: exec、edit、Playwright 调试

### 修复内容

1. ✅ `headless` 改为自动检测：`os.environ.get('DISPLAY') is None`
   - 服务器（无 DISPLAY）→ 无头模式
   - 本地电脑（有 DISPLAY）→ 有头模式
2. ✅ 登录状态检测增强：新增"立即登录"关键词检测
3. ✅ `wait_until='networkidle'` → `'domcontentloaded'`（SPA 兼容）
4. ✅ 登录超时从 120s 缩短到 60s
5. ✅ 登录失败后提前退出，不继续尝试后续商品
6. ✅ 浏览器连接断开检测（EPIPE 防护）
7. ✅ 清晰的错误提示：提示 Cookie 缺少 a1，建议在本地运行

### 测试结果

```
🔐 检测到未登录！
🔔 等待登录...
  仍在等待登录... (10s) ... (50s)
⏰ 登录超时
❌ 登录失败，跳过此商品
   提示：Cookie 中缺少 a1 字段，无法自动登录
   请在本地电脑运行本脚本，手动扫码登录一次
⚠️ 登录失败，后续商品无需继续尝试
```

zip 包已同步更新：`xianyu-publish.zip`

### 根本问题（未解决）

Cookie 缺少 HttpOnly 的 `a1` 字段，无法自动登录。
服务器 IP 被闲鱼风控拦截。
**需要老板在本地电脑运行，扫码登录一次即可。**


`hermes doctor` 验证配置是否正确
`hermes chat` 跟 hermes 对话

核心配置路径
/root/.hermes


设置主备模型，主模型挂了自动切备用模型：
```bash
# 添加主模型 + 备用模型
hermes fallback add custom --primary deepseek-chat --secondary qwen-max --tertiary llama-cpp

# 查看当前配置
hermes fallback list

# 测试故障转移链路
hermes fallback test
```

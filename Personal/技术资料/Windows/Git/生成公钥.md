---
tags:
  - windows
  - git
---


1、创建SSH Key公钥和私钥对

打开Git Bash，输入命令:`ssh-keygen -t rsa -C "panbin521@sina.com"`

输入完毕后按回车，到底即可

id_rsa和id_rsa.pub文件存放位置：

```
# windows
C:\Users\Administrator\.ssh
# linux
/root/.ssh
```

2、将SSH添加到版本管理仓库

测试GitHub连接：

```
ssh -T git@github.com
```